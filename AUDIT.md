| # | File | Location | Severity | Category | Description | Correct Fix |
|---|------|----------|----------|----------|-------------|-------------|
| 1 | `app/api/v1/reconcile/route.ts` | line 29-32 | Critical | Security | SQL injection + query breakage risk: interpolating `runId` and especially `parsed.notes` directly into SQL (`VALUES ('${runId}', '${parsed.notes ?? ''}', NOW())`). Any `'` in notes can break SQL; malicious input can inject. | Use parameterized queries / Drizzle insert builder. Never interpolate untrusted strings into SQL. |
| 2 | `app/api/v1/reconcile/route.ts` | line 50-52 | Critical | Security | SQL injection risk in GET: interpolating `id` into `WHERE id = '${id}'`. If `id` is attacker-controlled, this is exploitable. | Use parameterized query / Drizzle query builder (`where(eq(...))`). Validate `id` format and return 400 if missing/invalid. |
| 3 | `app/api/v1/reconcile/route.ts` | line 41-43 | Critical | Compliance | Returns `error.stack` to clients, leaking internals and potentially sensitive data (stack traces, SQL fragments, record contents) — bad under SOC 2/PCI posture. | Return a sanitized error message + stable error code; log detailed error server-side only. |
| 4 | `app/api/v1/reconcile/route.ts` | line 29-32, 34-38 | High | Logic | Creates a row in `reconciliation_runs` **before** running reconciliation, but never updates status (running/complete/failed). If reconciliation fails, DB contains a run that looks “created” but has no outcome, violating data integrity for “past runs.” | Wrap run creation + reconciliation + persistence in a transaction and update run status; or create as `running`, then set to `complete`/`failed` with error summary. |
| 5 | `app/api/v1/reconcile/route.ts` | line 40 | High | API | Response mixes two IDs (`runId` from `reconciliation_runs` and `result.id` from `reconciliations`) with no clear contract; clients won’t know which ID to use to fetch a run/report later. | Return a single canonical run identifier and consistently use it for retrieval; link reconciliation summary rows to that run ID. |
| 6 | `app/api/v1/reconcile/route.ts` | line 46-55 | High | API | GET requires `id` query param but the dashboard calls `/api/v1/reconcile` with no `id` (see UI). Also GET returns raw `runs` result, not `{ runs: [...] }` as the UI expects. | Implement `GET` as list endpoint (paginated) returning `{ runs }`, and optionally add `GET /api/v1/reconcile?id=...` (or `/api/v1/reconcile/:id`) for details. |
| 7 | `app/api/v1/reconcile/route.ts` | line 6-20 | Medium | API | Input schema doesn’t validate `valueDate` as datetime (`z.string()`), and doesn’t validate `currency` at all (USD-only requirement). Also doesn’t ensure `periodStart < periodEnd`. | Validate `valueDate` with `.datetime()` (or stricter ISO), enforce `currency === 'USD'` (for MVP), and add a refinement ensuring start < end. |
| 8 | `lib/services/reconciliation/reconciler.ts` | line 55-57 | High | Logic | Matching strategy is **amount-only**, which will frequently mis-match when multiple payments share the same amount. This produces incorrect reconciliation output and can corrupt payment statuses. | Match primarily on stable identifiers (e.g., `externalRef` ↔ `reference`/`transactionId`), then use amount/date as secondary signals; handle collisions deterministically and/or produce discrepancies requiring review. |
| 9 | `lib/services/reconciliation/reconciler.ts` | line 110, 150-156 | High | Logic | `discrepancies` is always returned empty; code never pushes any discrepancy items, violating the required report structure (“discrepancies should be flagged”). | Implement discrepancy detection (e.g., reference match but amount mismatch) and populate `discrepancies` accordingly. |
| 10 | `lib/services/reconciliation/reconciler.ts` | line 129-134 | High | Logic | `totalBankAmount` sums **all uploaded bank records**, even those outside the requested period (loop filters by period for matching, but totals don’t). This makes summary totals/difference incorrect. | Filter bank records to the period first (and likely to USD) and compute totals from the filtered set used for reconciliation. |
| 11 | `lib/services/reconciliation/reconciler.ts` | line 114-126 | High | Logic | If multiple system payments share the same amount, `findMatch` picks the first, causing non-deterministic/incorrect pairing (depends on DB ordering). | Add deterministic ordering + tie-breakers, or treat ambiguous matches as discrepancies requiring manual review. |
| 12 | `lib/services/reconciliation/reconciler.ts` | line 83-95 | High | Logic | `markReconciled` only updates when status is `'pending'`, but reconciliation should typically apply to `'cleared'` (or another “settled” status) — current logic may fail to mark legitimately reconcilable payments, or marks the wrong lifecycle state. | Align with domain status model: reconcile only eligible statuses (likely `cleared`), and record reconciliation metadata rather than overwriting status blindly. |
| 13 | `lib/services/reconciliation/reconciler.ts` | line 83-95, 114-126, 136-149 | High | Logic | No transaction around: marking payments reconciled (updates) and inserting reconciliation summary. Partial updates can occur if the function crashes mid-run, leaving payment statuses changed without a persisted reconciliation record (data integrity risk). | Use a DB transaction for the entire reconciliation run: compute matches, persist run + summary, then update payment statuses atomically. |
| 14 | `lib/services/reconciliation/reconciler.ts` | line 83-95 | Medium | Performance | N+1 query pattern: for each matched payment, it does a `select` then (maybe) `update`. This will not scale with larger batches. | Update in bulk (single update with `where in (...)`), or select once and update only eligible rows without per-row round trips. |
| 15 | `lib/services/reconciliation/reconciler.ts` | line 118-120 | Medium | Performance | O(\(n^2\)) behavior: each bank record recomputes `remaining = systemPayments.filter(...)`, then `find` over it. Large batches will be slow. | Pre-index system payments (e.g., Map keyed by reference and/or amount) and do O(\(n\)) matching with collision buckets. |
| 16 | `lib/services/reconciliation/reconciler.ts` | line 69-71, 115-117 | Medium | Logic | `parseBankDate` accepts any string; invalid dates become `Invalid Date`, and `isInPeriod` comparisons will silently evaluate false, dropping records without explanation. | Validate `valueDate` format upfront (API schema) and/or detect invalid Date and surface it as an input error/discrepancy. |
| 17 | `lib/services/reconciliation/reconciler.ts` | line 62-64, 107-108 | Medium | Logic | Period boundary semantics mismatch: matching uses `date < periodEnd` (exclusive), but DB query uses `between(...)` (typically inclusive of both ends). This can yield inconsistent inclusion between bank records and system payments. | Use consistent half-open interval semantics everywhere (e.g., `createdAt >= start AND createdAt < end`) and mirror the same for bank dates. |
| 18 | `components/reconciliation/ReconciliationDashboard.tsx` | line 28-38 vs API GET | High | API | UI expects `GET /api/v1/reconcile` to return `{ runs: [...] }`, but the API returns a raw DB result and (as written) needs an `id` query param. This makes the dashboard always show an empty list. | Align the endpoint contract: implement a list endpoint returning `{ runs }`, and update UI if needed to match the actual response shape. |
| 19 | `components/reconciliation/ReconciliationDashboard.tsx` | line 28-38 | Medium | Performance | Interval is never cleared on unmount; this leaks timers and keeps polling after navigation. | Return a cleanup function from `useEffect` that calls `clearInterval(interval)`. |
| 20 | `components/reconciliation/ReconciliationDashboard.tsx` | line 30-36 | Medium | API | Doesn’t check `res.ok` before `res.json()`. Non-2xx responses can throw, and errors are silently swallowed, masking real failures and complicating ops. | Check `res.ok`, handle/display an error state, and optionally backoff/retry intelligently. |
